# TYPO3 projects convention checker

## Install
0. Require package via composer `composer require --dev "staempfli/typo3-conventions-checker"`
0. Commit changes via command line. All checks will be run automagically.